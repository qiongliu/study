let a = 20;

class Person {
	constructor (name,age) {
		this.name = name;
		this.age = age;
	}
	getInfo () {
		console.log(this.name,this.age);
	}
}

exports.a = a;

module.exports = Person;

// (function(require,exports,module){
// 	对外暴露的接口是 module.exports 
// 	exports 是一个局部变量，指向 module.exports 的引用
// 	注意：可以给 exports 添加属性，不要改变其引用
// })();